import { over } from "./over";

it("ok sync", () => {});
it("ok async", async () => {});
it("error sync", () => {});
it("error async", async () => {});

it("should be true", () => expect(true).toBe(true));
